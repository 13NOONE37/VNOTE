import React from 'react';

export default function NotFound() {
  return (
    <div
      style={{
        width: '100%',
        color: '#fff',
        display: 'grid',
        placeItems: 'center',
      }}
    >
      <h1>There is no that page</h1>
    </div>
  );
}
